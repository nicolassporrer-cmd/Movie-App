# Generated files — never edit directly
*.local.html

# Session data
payloads/
transcripts/

# Credentials
.env
*.env.local

# Plans — TRACKED on purpose: they are part of the rebuild spec (see JOURNAL.md)

# Claude local settings
.claude/settings.local.json
.claude/last_anchor_check

# Node / npm
node_modules/

# Letterboxd exports — personal data, never commit
data/raw/
*.zip

# Build output
dist/
