{
  "name": "movie-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "data": "npm run data:build && npm run data:sync && npm run data:providers",
    "data:build": "node --max-old-space-size=6144 scripts/build-data.cjs",
    "data:sync": "node scripts/sync-letterboxd.cjs",
    "data:providers": "node scripts/apply-providers.cjs",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
